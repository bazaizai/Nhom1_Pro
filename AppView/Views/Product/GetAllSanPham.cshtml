@model IEnumerable<Nhom1_Pro.Models.Product>

@{
    ViewData["Title"] = "GetAllSanPham";
}

<h1>GetAllSanPham</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ten)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ten)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrangThai)
            </td>
            <td>
                @Html.ActionLink("Edit", "EditSp", new {id=item.Id }) |
                    @Html.ActionLink("Details", "DetailSp", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "DeleteSP", new { id=item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
